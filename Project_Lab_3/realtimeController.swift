//This controller was meant to allow the user to see their vitals in realtime. That, unfortunately, never materialized due to hardware compatibility issues that will hopefully be fixed in future iterations. The aim of the project was, however, completed by creating a dummy app that demonstrate the capibility to transfer data from the given hardware to the MAC
//  realtimeController.swift
//  Project_Lab_3
//
//  Created by Folusakin Abiola on 11/9/21.
//

import Foundation

import UIKit

class realtimeController: UIViewController{
    override func viewDidLoad() {
        super.viewDidLoad()
//        emailField.delegate = self
//        passwordField.delegate = self
    }

}
